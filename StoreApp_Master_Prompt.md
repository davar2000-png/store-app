# پرامپت اصلی پروژه — نرم‌افزار حسابداری فروشگاه (نسخه گیت‌هاب)

این فایل را در چت جدید Claude پیست کن، و فقط همین یک جمله را زیرش اضافه کن:

> **«مرحله .... را بساز»** (به‌جای نقطه‌چین، شماره مرحله را بنویس، مثلاً «مرحله ۸ را بساز»)

نیازی به آپلود فایل زیپ نیست — همه‌چیز از گیت‌هاب خوانده می‌شود.

---

## دستور مستقیم به Claude

کد کامل و به‌روز پروژه در این مخزن گیت‌هاب عمومی قرار دارد:

**`https://github.com/davar2000-png/store-app`**

لطفاً اول با دستور زیر (یا مشابه آن) آخرین نسخه‌ی فایل زیپ پروژه را از این مخزن دانلود و از حالت فشرده خارج کن تا به کد فعلی دسترسی مستقیم داشته باشی:

```bash
cd /home/claude
curl -L -o project.zip https://github.com/davar2000-png/store-app/archive/refs/heads/main.zip
# یا اگر فایل zip مستقیم داخل مخزن آپلود شده (نه سورس گیت):
# فایل‌های داخل مخزن را با ls یا از طریق GitHub API لیست کن و فایل zip موجود را دانلود کن
```

بعد از دسترسی به کد، پوشه `StoreApp` را باز کن، فایل‌های `README_PhaseN.md` موجود را ببین تا بفهمی **کدام مراحل قبلاً ساخته شده‌اند**، سپس دقیقاً مرحله‌ای که کاربر در پیامش خواسته را بساز.

---

## ۱. معرفی پروژه

نرم‌افزار حسابداری و مدیریت فروشگاه موبایل، لپ‌تاپ و کنسول بازی، طبق یک سند نیازمندی ۴۰ بخشی.
**کاربر مبتدی است** (بدون تجربه برنامه‌نویسی) — همیشه باید:
- مرحله‌به‌مرحله، ساده، و کاملاً فارسی توضیح داده شود.
- در هر پیام فقط یک کار خواسته شود؛ بعد از تأیید موفقیت، برو سراغ بعدی.
- کدهای جدید همیشه به‌صورت فایل ساخته و zip قابل‌دانلود تحویل داده شود (هرگز کد بلند داخل چت پیست نشود).

## ۲. تصمیمات فنی نهایی (تغییرناپذیر)

| مورد | مقدار |
|---|---|
| سیستم‌عامل هدف | Windows 11 |
| دیتابیس | SQL Server Express — نام instance: `.\SQLEXPRESS` |
| زبان و فریم‌ورک | Python + PyQt6 (رابط کاربری فارسی، راست‌چین) |
| اتصال دیتابیس | pyodbc |
| تاریخ | jdatetime (شمسی) |
| نام دیتابیس نرم‌افزار جدید | `StoreAppDB` |
| نام دیتابیس نرم‌افزار قبلی (ربات، فقط برای Import) | `RoboAccDB` (از قبل روی سرور کاربر Restore شده) |

## ۳. الگوهای معماری که باید در مراحل بعد هم رعایت شوند

- **لایه‌بندی:** `database/` (دسترسی خام SQL) ← `services/` (منطق تجاری، تراکنش‌های اتمیک) ← `ui/` (فقط نمایش).
- **گسترش دیتابیس:** هر مرحله جدید یک فایل SQL جداگانه می‌سازد (مثلاً `phase8_xxx.sql`) با الگوی `IF OBJECT_ID(...) IS NULL` — هرگز فایل‌های SQL قبلی بازنویسی/حذف نمی‌شوند.
- **FIFO:** منطق در `utils/fifo.py` — `add_purchase_layer()`, `add_cardex_entry()`, `consume_fifo()`.
- **کاردکس:** جدول `ProductCardex`. **موجودی جاری:** ستون `Products.CurrentStock`. **سریال/IMEI:** جدول `ProductSerials`.
- **تحویل فایل:** هر مرحله، کل پوشه `StoreApp` (نه فقط فایل‌های جدید) در یک zip تحویل داده می‌شود + یک `README_PhaseN.md` جداگانه.
- **بعد از ساخت هر مرحله، به کاربر یادآوری کن** که فایل zip جدید را در همان مخزن گیت‌هاب (جایگزین فایل قبلی) آپلود کند تا برای چت بعدی آماده باشد.

## ۴. اطلاعات دیتابیس ربات (برای مرحله Import)

بک‌آپ ربات با موفقیت Restore شده (نام دیتابیس: `RoboAccDB`، ۶۹ جدول). نگاشت کلی:

| جدول ربات | معادل جدید |
|---|---|
| `Customers`, `DetailAccounts` | اشخاص |
| `Products`, `ProductGroups`, `ProductUnits`, `ProductCardex` | کالا و انبار |
| `Factors` (ستون `TypeRef`), `ProductItems` | خرید/فروش |
| `ChequeRecs`, `ChequePays` | چک‌ها |
| `Cashs`, `CashBox`, `BankAccounts`, `BankMovements` | دریافت/پرداخت/بانک |
| `TypeRefs` | کدهای نوع تراکنش (2=خرید, 3=فروش, 4=برگشت فروش, 5=برگشت خرید, 15=پیش‌فاکتور فروش, 27=چک دریافتی, 36=چک پرداختی, 37=اقساط) |

مرحله ۷ (Import) یک رابط «نگاشت ستون» دارد که کاربر خودش ستون‌ها را انتخاب می‌کند — نیازی به فرض نام دقیق ستون نیست.

## ۵. اولویت‌بندی مراحل (طبق سند اصلی) — وضعیت تا این لحظه

| # | مرحله | وضعیت |
|---|---|---|
| ۱ | هسته (دیتابیس، کاربران، اشخاص، کالا) | ✅ تست شده |
| ۲ | خرید و انبار (FIFO، کاردکس، سریال) | ✅ تحویل شده |
| ۳ | فروش (فاکتور، پیش‌فاکتور، برگشت، تسویه، چاپ A5) | ✅ ساخته شده |
| ۴ | مالی (دریافت، پرداخت، بانک، چک، اقساط) | ✅ اجرا و تست شده |
| ۵ | گزارش‌ها (۸ نوع گزارش) | ✅ دانلود، اجرا و تست شده |
| ۶ | ارتباط با مشتری (پیامک، بله) | ✅ ساخته شده — کاربر فعلاً فعالش نکرده |
| ۷ | Import از ربات + Backup/Restore | ✅ تحویل شده |
| ۸ | دستیار هوش مصنوعی | وضعیت را از روی فایل‌های `README_PhaseN.md` موجود در مخزن بررسی کن |
| ۹ | توسعه‌های آینده | — |

**نکته:** چون این فایل ممکن است با تأخیر بعد از چند مرحله دوباره استفاده شود، جدول بالا ممکن است قدیمی شده باشد — همیشه وضعیت واقعی را از روی فایل‌های `README_PhaseN.md` داخل مخزن گیت‌هاب (بالاترین شماره = آخرین مرحله‌ی انجام‌شده) تشخیص بده، نه فقط از این جدول.

## ۶. دستورالعمل نهایی

۱. کد را از گیت‌هاب بخوان.
۲. آخرین مرحله‌ی انجام‌شده را از روی فایل‌های README تشخیص بده.
۳. اگر کاربر شماره مرحله‌ای خواسته که به مرحله‌ی قبلش (که هنوز تست نشده) وابسته است، یک هشدار کوتاه بده ولی در صورت اصرار کاربر همکاری کن.
۴. الگوهای معماری بخش ۳ را دقیقاً رعایت کن.
۵. در پایان، کل پوشه `StoreApp` به‌روزشده را zip کن، `README_PhaseN.md` مخصوص همان مرحله بساز، تحویل بده، و به کاربر یادآوری کن که این zip جدید را در گیت‌هاب آپلود کند.
