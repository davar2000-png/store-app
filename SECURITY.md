# SECURITY.md

## Password Hashing
پسورد کاربران با SHA-256 + Salt تصادفی هش می‌شود (`utils/security.py`).
قابل قبول است، اما در فازهای بعدی می‌توان به `bcrypt` یا `argon2` ارتقا داد
که در برابر حملات Brute-force مقاوم‌تر هستند.

## SQL Server Authentication
اتصال دیتابیس پیش‌فرض بر پایه Windows Authentication است (`config.py`)،
نه رمز عبور Hardcode شده. مناسب است.

## Connection Security
Connection String داخل کد Hardcode نشده. توصیه می‌شود در فازهای بعدی
از فایل `.env` (که در `.gitignore` قرار دارد) برای هر مقدار حساس احتمالی
استفاده شود.

## 🔴 GitHub Backup Risk — یافته امنیتی مهم
دو فایل Backup واقعی SQL Server به‌صورت تصادفی وارد این Repository عمومی شده بودند:

| فایل | وضعیت فعلی |
|---|---|
| `TECHNOKALARoboAccBackUp_1405-05-22-15-48.zip` | از نسخه فعلی حذف شده، **ولی در Git History باقی است** |
| `TECHNOKALARoboAccBackUp_1405-05-23-09-50.zip` (۲۷MB) | از نسخه فعلی حذف شده (توسط کاربر)، **ولی در Git History باقی است** |

**وضعیت:** حذف کامل این دو فایل از تاریخچه نیاز به `git filter-repo` +
Force Push دارد. کاربر تصمیم گرفت این کار را فعلاً به تعویق بیندازد.
تا زمانی‌که این کار انجام نشود، هرکسی که کل تاریخچه Repository را
مشاهده کند (نه فقط آخرین نسخه) می‌تواند این فایل‌ها را دانلود کند.

**توصیه:** اگر این فایل‌ها داده واقعی مشتریان/مالی داشته‌اند، رمزها یا
اطلاعات حساس داخل آن‌ها Reset/Rotate شوند، صرف‌نظر از اینکه تاریخچه
پاکسازی شود یا نه — چون در بازه‌ای که این فایل‌ها عمومی بودند، امکان
دانلود توسط هرکسی وجود داشت.

## Secrets
هیچ رمز یا کلید API داخل کد پیدا نشد.

## Environment Variables
فعلاً استفاده نمی‌شود؛ `config.py` مقادیر ثابت (غیرحساس) نگه می‌دارد.

## قانون طلایی
**Database Backup واقعی هرگز نباید در GitHub عمومی نگهداری شود.**
جزئیات محل صحیح نگهداری Backup در `BACKUP_POLICY.md`.
