-- =========================================================
-- Phase 15.4 — دریافت وجه (Receipt) → Double-Entry Accounting Ledger
-- =========================================================
-- زمینه (چرا این Migration لازم شد):
-- اتصال سند دریافت وجه (create_receipt) به Ledger با یک ابهام تجاری
-- روبه‌رو بود: دریافت‌های تخصیص‌نیافته (Partially Allocated) — یعنی
-- زمانی که مجموع تخصیص به فاکتور(های) فروش (alloc_sum) کمتر از مبلغ کل
-- دریافتی (total_amount) است. طبق تصمیم صریح Option F.2:
--
--   دریافت‌های Partially Allocated/Unallocated رد نمی‌شوند (رفتار فعلی
--   create_receipt() که این حالت را مجاز می‌داند دست‌نخورده می‌ماند).
--   مبلغ تخصیص‌نیافته، اقتصادی به معنای «پیش‌دریافت از مشتری» (Customer
--   Advance) است — بدهی شرکت به مشتری برای کالا/خدماتی که هنوز تحویل
--   نشده — و باید در یک حساب بدهی (Liability) مجزا از 1100 (حساب‌های
--   دریافتنی؛ طلب شرکت از مشتری، ماهیت دقیقاً معکوس) ثبت شود.
--
--   2300 دریافت علی‌الحساب مشتریان (Customer Advances)
--        — ماهیت Liability/Credit؛ فقط برای مانده تخصیص‌نیافته دریافت
--        استفاده می‌شود، هرگز برای جایگزینی 1100. هیچ حساب پیش‌دریافت
--        دیگری ساخته نمی‌شود (طبق تصمیم صریح، از اختراع حساب دوم
--        خودداری شده است).
--
-- ParentRef این حساب عمداً NULL است — دقیقاً هم‌سو با الگوی 011_purchase_
-- accounting.sql: در Chart of Accounts فعلی هیچ حساب Seed‌شده‌ای ParentRef
-- غیر NULL ندارد و هیچ بخشی از کد به آن برای Rollup یا اعتبارسنجی متکی
-- نیست.
--
-- حساب‌های 1000 (صندوق و بانک)، 1100 (حساب‌های دریافتنی) و 1300 (اسناد
-- دریافتنی/چک‌های نزد ما) از قبل در 009_accounting_core.sql Seed شده‌اند
-- و در این Migration دوباره ساخته/تغییر داده نمی‌شوند.
--
-- امن و غیرمخرب: حساب فقط در صورت نبودن Code آن اضافه می‌شود؛ هیچ حساب
-- موجودی حذف/ویرایش نمی‌شود و هیچ Backfill انجام نمی‌شود.
-- =========================================================

USE StoreAppDB;
GO

IF NOT EXISTS (SELECT 1 FROM ChartOfAccounts WHERE Code = N'2300')
    INSERT INTO ChartOfAccounts (Code, Name, AccountType, NormalBalance)
    VALUES (N'2300', N'دریافت علی‌الحساب مشتریان', N'Liability', N'Credit');
GO
