-- =========================================================
-- Phase 15.5 — پرداخت وجه (Payment) → Double-Entry Accounting Ledger
-- =========================================================
-- زمینه (چرا این Migration لازم شد):
-- اتصال سند پرداخت وجه (create_payment) به Ledger با همان ابهام تجاری
-- Phase 15.4 روبه‌رو بود: پرداخت‌های تخصیص‌نیافته (Partially Allocated) —
-- یعنی زمانی که مجموع تخصیص به فاکتور(های) خرید (alloc_sum) کمتر از
-- مبلغ کل پرداختی (total_amount) است. طبق تصمیم صریح Phase 15.5:
--
--   پرداخت‌های Partially Allocated/Unallocated رد نمی‌شوند (رفتار فعلی
--   create_payment() که این حالت را مجاز می‌داند دست‌نخورده می‌ماند).
--   مبلغ تخصیص‌نیافته، اقتصادی به معنای «پیش‌پرداخت به تأمین‌کننده»
--   (Supplier Advance) است — دارایی شرکت نزد تأمین‌کننده برای کالا/
--   خدماتی که هنوز دریافت نشده — و باید در یک حساب دارایی (Asset) مجزا
--   از 2000 (حساب‌های پرداختنی؛ بدهی شرکت به تأمین‌کننده، ماهیت دقیقاً
--   معکوس) ثبت شود.
--
--   1500 پیش‌پرداخت به تأمین‌کنندگان (Supplier Advances)
--        — ماهیت Asset/Debit؛ فقط برای مانده تخصیص‌نیافته پرداخت
--        استفاده می‌شود، هرگز برای جایگزینی 2000. هیچ حساب پیش‌پرداخت
--        دیگری ساخته نمی‌شود (طبق تصمیم صریح، از اختراع حساب دوم
--        خودداری شده است).
--
-- ParentRef این حساب عمداً NULL است — دقیقاً هم‌سو با الگوی 011_purchase_
-- accounting.sql و 012_receipt_accounting.sql: در Chart of Accounts فعلی
-- هیچ حساب Seed‌شده‌ای ParentRef غیر NULL ندارد و هیچ بخشی از کد به آن
-- برای Rollup یا اعتبارسنجی متکی نیست.
--
-- چک صادرشده (Issued) توسط create_payment به 2100 (اسناد پرداختنی) اعتبار
-- می‌شود، نه 1000 — چون فرایند عملیاتی فعلی صدور چک، موجودی صندوق/بانک
-- را کم نمی‌کند؛ تسویه واقعی چک از طریق change_cheque_status، خارج از
-- محدوده این فاز است.
--
-- حساب‌های 1000 (صندوق و بانک)، 2000 (حساب‌های پرداختنی) و 2100 (اسناد
-- پرداختنی) از قبل در 009_accounting_core.sql Seed شده‌اند و در این
-- Migration دوباره ساخته/تغییر داده نمی‌شوند.
--
-- امن و غیرمخرب: حساب فقط در صورت نبودن Code آن اضافه می‌شود؛ هیچ حساب
-- موجودی حذف/ویرایش نمی‌شود و هیچ Backfill انجام نمی‌شود.
-- =========================================================

USE StoreAppDB;
GO

IF NOT EXISTS (SELECT 1 FROM ChartOfAccounts WHERE Code = N'1500')
    INSERT INTO ChartOfAccounts (Code, Name, AccountType, NormalBalance)
    VALUES (N'1500', N'پیش‌پرداخت به تأمین‌کنندگان', N'Asset', N'Debit');
GO
