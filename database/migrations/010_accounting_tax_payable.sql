-- =========================================================
-- Phase 15.2 — Accounting: Sales ↔ Ledger Connection
-- =========================================================
-- زمینه (چرا این Migration لازم شد):
-- در بررسی اتصال فروش به Ledger (Phase 15.2) مشخص شد که TaxAmount یک
-- فیلد واقعی و فعال در فاکتور فروش/خرید است (UI هم برای آن ورودی دارد:
-- ui/sales_window.py::tax_input) اما Chart of Accounts ساخته‌شده در
-- Phase 15.1 هیچ حساب بدهی (Liability) برای مالیات دریافت‌شده از مشتری
-- نداشت. بدون این حساب، سند حسابداری فروش در مواردی که TaxAmount > 0
-- باشد، یا باید حدسی به یک حساب نامرتبط بستانکار می‌شد یا اصلاً موازنه
-- نمی‌شد — هر دو طبق قانون Brief («در صورت ابهام حدس نزن») غیرقابل قبول
-- بود. راه‌حل حداقلی: افزودن یک حساب بدهی جدید برای مالیات دریافتی از
-- مشتری، دقیقاً مثل بقیه حساب‌های Seed شده در 009_accounting_core.sql.
--
-- امن و غیرمخرب: فقط در صورت نبودن Code = 2200 آن را اضافه می‌کند؛ هیچ
-- جدول یا داده موجودی را تغییر نمی‌دهد.
-- =========================================================

USE StoreAppDB;
GO

IF NOT EXISTS (SELECT 1 FROM ChartOfAccounts WHERE Code = N'2200')
    INSERT INTO ChartOfAccounts (Code, Name, AccountType, NormalBalance)
    VALUES (N'2200', N'مالیات بر ارزش‌افزوده دریافتنی از مشتری (پرداختنی به سازمان مالیاتی)',
            N'Liability', N'Credit');
GO
